<p style="color: green"><%= notice %></p>

<%= render @project %>

<%= @admin and render partial: "edit" %>

<h2> Contributors: </h2>

<ul>
  <% @project.users.each do |user|  %>
    <li><%= link_to user.name, user_path(user) %></li>
  <% end %>
</ul>
