<div class="row-fluid">
  <div class="span3">
    <h1 id='talkheader'>Come talk with us!</h1>

    <br/>

    <p class="lead">
      If there is anything we love, it is learning and creating. We would
      really love it if you shared your knowledge and/or your experiences
      with us.
    </p>
    <%= link_to 'Suggest a Topic For a Talk', suggestions_path, {:style => 'font-size 16px; font-weight: bold'} -%>

  </div>
  <div class="span9" id='content'>
    <div class='row-fluid' id='tabs'>
      <div class='span3' id='upcoming-talks'>
        <a href="#upcoming">Upcoming Talks</a>
      </div>
      <div class='span3' id='past-talks'>
        <a href="#past">Past Talks</a>
      </div>
      <div class='span3' id='proposed-talks'>
        <a href="#proposed">Proposed Talks</a>
      </div>
    </div>

    <section id="upcoming">
      <div class="talk-box">
        <% @talks.each do |talk| %>
          <div class="row-fluid">
            <div class="talk span9">
              <h4><%= link_to "#{talk.title}", talk_path(talk) %></h4>
              <div><%= indefinite_Articlerize talk.kind %> talk proposed by <%= talk.presenter %></div>
              <p class="description"><%= talk.description %></p>
            </div>
          </div>
        <% end %>
      </div>
    </section>

    <section id="past" class="hide">
      <div class="talk-box">
         <% @past_talks.each do |talk| %>
          <div class="row-fluid">
            <div class="talk span9">
              <h4><%= link_to "#{talk.title}", talk_path(talk) %></h4>
              <div><%= indefinite_Articlerize talk.kind %> talk proposed by <%= talk.presenter %></div>
              <p class="description"><%= talk.description %></p>
            </div>
          </div>
        <% end %>
      </div>
    </section>

    <section id="proposed" class="hide">
      <div class="talk-box">
         <% @proposed_talks.each do |talk| %>
          <div class="row-fluid">
            <div class="talk span9">
              <h4><%= link_to "#{talk.title}", talk_path(talk) %></h4>
              <div><%= indefinite_Articlerize talk.kind %> talk proposed by <%= talk.presenter %></div>
              <p class="description"><%= talk.description %></p>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  </div>

</div>

<hr>
<div class="row-fluid">
  <div class="span8">
    <%= render 'form' %>

    <p>
      <small>
        This is nerd focused... Not marketing-focused. Not
        sales-focused. Not recruitment-focused. If you want to talk
        about your company, just make sure you talk
        about <u>using</u> your product. Something we can share.
      </small>
    </p>
  </div>
</div>